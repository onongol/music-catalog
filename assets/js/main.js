// Точка входа сборки. Своего кода здесь нет: файл существует ради стилей.
// Дев-сервер Vite отдаёт CSS-энтрипоинт как ES-модуль, и тег <link> такой
// ответ не принимает, поэтому стили подключаются импортом из JS — тогда
// работают оба режима: в дев-режиме модуль подставляет стили и обновляет их
// по HMR, в сборке Vite выносит их в отдельный файл, а django-vite добавляет
// на него <link> рядом со <script>.
import "../css/input.css";
