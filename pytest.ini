[pytest]
DJANGO_SETTINGS_MODULE = config.settings
; tests.py — стандартное имя файла тестов в Django-приложении,
; test_*.py — файлы внутри пакета catalog/tests/.
python_files = test_*.py tests.py
addopts = --tb=short -q
